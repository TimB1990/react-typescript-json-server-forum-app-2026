## React + typesript + vite
## Forum application 2026

# How to start
This comment will both run JSON server and the react application using concurrency (see package.json)
```bash
npm run dev
```

# TODOS
- To make structure more dynamic. Instead of fixed entities like group and category refactor to category having a parent_id. This way we can stack subjects.
